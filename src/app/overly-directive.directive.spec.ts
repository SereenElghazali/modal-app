import { OverlyDirectiveDirective } from './overly-directive.directive';

describe('OverlyDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new OverlyDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
